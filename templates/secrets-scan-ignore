# secrets-scan-ignore — King Framework
# Archivos y patrones excluidos del escaneo de secretos (CASTLE S gate)
#
# FORMATO:
#   - Una exclusión por línea
#   - Líneas que empiezan con # son comentarios (ignoradas)
#   - Paths relativos al root del proyecto (globs soportados: *, **)
#   - PATTERN:<regex> excluye un patrón regex específico del escaneo
#
# UBICACIÓN: .king/secrets-scan-ignore en tu proyecto
# Generado por /genesis. Modificable manualmente.
#
# Si este archivo no existe, el gate corre sin exclusiones (comportamiento seguro por defecto).

# ── Documentación del framework ──────────────────────────────────────────────
# Estos archivos contienen ejemplos de patrones de secretos (para ilustrar el gate)
# y dispararían falsos positivos si se escanearan.
king-framework/security/checks/secrets.md
king-framework/security/SECURITY-GATE.md
king-framework/knowledge/_inject/security-essentials.md
king-framework/knowledge/_inject/secrets-management.md
king-framework/rules/security/no-secrets.md

# ── Sesiones de King (.king/) ─────────────────────────────────────────────────
# Las sesiones pueden capturar contexto externo con ejemplos de secretos.
.king/sessions/

# ── Tests y mocks ──────────────────────────────────────────────────────────────
# Pueden contener valores de prueba que coinciden con patrones reales.
*.test.*
*.spec.*
*.snap
**/__tests__/**
**/test/**
**/tests/**
**/mocks/**
**/fixtures/**

# ── Archivos de ejemplo y templates ──────────────────────────────────────────
# Templates y ejemplos documentan el formato de secretos, no son secretos reales.
*.example
*.sample
*.template
**/templates/**

# ── Exclusiones de patrones regex específicos ─────────────────────────────────
# Formato: PATTERN:<regex>
# Usar cuando un placeholder o valor de prueba coincide exactamente con un patrón.
# Ejemplos (descomentar según necesidad):
# PATTERN:your-api-key-here
# PATTERN:sk_(live|test)_EXAMPLE
# PATTERN:AKIAIOSFODNN7EXAMPLE
