# King Framework — Security Gate Exceptions
# Use this file to document accepted security exceptions with expiry dates
# DO NOT commit real secrets here. Only document WHY an exception was accepted.
#
# Generated: 2026-03-18
# This file is created from exceptions.yml.template
# Add actual exceptions below as needed.

exceptions:
  - id: "EXC-001"
    rule: "secret-scan/password-keyword"
    path: "king-framework/skills/dr-setup/RUNBOOK-TEMPLATE.md"
    line: 80
    pattern: "--password=\"${DB_PASS}\""
    reason: "MySQL CLI restore command uses --password= flag with ${DB_PASS} env var reference. Value is never hardcoded — expands to runtime env var. Required by MySQL CLI protocol; no secret stored in the file."
    approved_by: "InformatiK-AI"
    commit: "0d3c182"
    expires: "2026-11-07"
    ticket: "#92"
  # Example:
  # - id: "EXC-002"
  #   rule: "secrets_detection"
  #   path: "src/tests/fixtures/mock-token.ts"
  #   reason: "Test fixture uses fake token format for unit testing"
  #   approved_by: "@security"
  #   expires: "2026-06-01"
  #   ticket: "#123"
